[program:puma-{{ redmine_instance.name }}]
command=bundle exec puma --config config/puma.rb
numprocs=1
directory={{ redmine.app_base_path}}/{{ redmine_instance.name }}
environment=PATH={{ redmine.app_base_path}}/{{ redmine_instance.name }}/vendor/bundle/ruby/2.0.0/bin:{{ lookup('env', 'PATH') }}"
user=redmine

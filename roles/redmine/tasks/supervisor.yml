---

- name: "Install Supervisor Conf for Puma/Redmine"
  template:
    src: redmine-puma-supervisor.conf.j2
    dest: "/etc/supervisor/conf.d/puma-{{ redmine.name }}.conf"
    owner: root
    group: root
    mode: "u=rwx,g=rx,o=rx"
  notify:
    - "Restart Supervisor"
